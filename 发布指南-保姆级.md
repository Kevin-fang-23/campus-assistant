# 校园事务智能助手 · 发布保姆级教程

> 目标：把项目变成招聘人员「点开就能用」的在线链接，同时附带一个可浏览代码的 GitHub 仓库。
> 全程免费，大约 30～60 分钟。你项目里的部署配置已经全部备好，照着填即可。

---

## 整体流程（5 步）

```
第 0 步 准备工作（注册账号 + 生成令牌）
第 1 步 代码推到 GitHub
第 2 步 后端部署到 Render（免费）
第 3 步 前端部署到 Vercel（免费）
第 4 步 回填跨域（CORS）+ 验收
第 5 步 补演示材料 + 发给招聘人员
```

需要 3 个账号：**GitHub**（必须）、**Render**（后端）、**Vercel**（前端）。后两个都可以直接用 GitHub 账号一键登录。

---

## 第 0 步：准备工作（一次性，约 10 分钟）

### 0.1 注册 GitHub 账号
1. 打开 https://github.com ，点右上角 **Sign up**。
2. 按提示填邮箱、密码、用户名（用户名会出现在链接里，建议用拼音/英文，如 `zhangsan`）。
3. 邮箱验证后即可使用。

> 你的电脑已安装 git（版本 2.55.0），无需再装。

### 0.2 生成 GitHub 令牌（Personal Access Token，push 代码必须）
> 从 2021 年起 GitHub 禁止用「账号密码」推送代码，必须用令牌（token）或 SSH。令牌最简单。

1. 登录 GitHub，点右上角头像 → **Settings**。
2. 左侧栏拉到底 → **Developer settings**。
3. 选 **Personal access tokens** → **Tokens (classic)**（或 Fine-grained tokens，任选）。
4. 点 **Generate new token** → **Generate new token (classic)**。
5. 填写：
   - **Note**：随便填，如 `campus-deploy`。
   - **Expiration**：选 90 天（够用）或 No expiration。
   - **Select scopes**：勾选 **`repo`**（整个 repo 大类勾上即可）。
6. 点最下方 **Generate token**。
7. **立刻复制**那一串 `ghp_xxxx...` 保存好（页面关了就再也看不到了）。这就是你以后 push 时的「密码」。

---

## 第 1 步：把代码推到 GitHub（约 10 分钟）

### 1.1 在 GitHub 新建一个空仓库
1. 登录 GitHub，点右上角 **+** → **New repository**。
2. **Repository name**：`campus-assistant`。
3. **Public**（公开，招聘人员才能看）；**不要**勾选 Add a README / .gitignore / license（保持空仓库）。
4. 点 **Create repository**。创建后页面会显示仓库地址：
   ```
   https://github.com/<你的用户名>/campus-assistant.git
   ```

### 1.2 在本地打开命令行
- 按 `Win + R`，输入 `cmd`，回车（或在项目文件夹空白处右键 → 在终端中打开）。
- 先进入项目目录（把路径换成你自己的）：
  ```bat
  cd /d C:\Users\86182\WorkBuddy\2026-08-24-17-13-36\campus-assistant
  ```

### 1.3 初始化并提交（逐条粘贴执行）
```bat
git init
git config --global user.name  "你的名字（可用拼音）"
git config --global user.email "你注册 GitHub 的邮箱"

git add .
git commit -m "feat: 校园事务智能助手 MVP"

git branch -M main
git remote add origin https://github.com/<你的用户名>/campus-assistant.git
git push -u origin main
```

### 1.4 push 时的身份验证
第一次 push 会要求登录，二选一：
- **方式 A（推荐）**：会弹出一个 GitHub 登录窗口（Windows 的 git 自带凭据管理器），点浏览器授权即可，之后自动记住。
- **方式 B（手动）**：命令行提示 `Username` 时填你的 GitHub **用户名**；提示 `Password` 时**粘贴第 0.2 步复制的令牌**（不是账号密码）。

> 验证成功：命令行出现 `Writing objects ... done`，刷新 GitHub 网页能看到你的代码和 README 就对了。

---

## 第 2 步：部署后端到 Render（约 10 分钟）

### 2.1 注册并登录
1. 打开 https://render.com ，点 **Get Started**，选择 **Sign in with GitHub** 授权登录。

### 2.2 创建后端服务（推荐用 Blueprint，一键读配置）
你项目根目录已经有 `render.yaml`，用它最省事：
1. 点顶部 **New +** → **Blueprint**。
2. 授权 Render 访问你的 GitHub，选择 `campus-assistant` 仓库。
3. Render 自动读取 `render.yaml`，识别出一个 Web Service `campus-assistant-backend`，直接点 **Apply / Deploy**。

> 如果 Blueprint 找不到，就改手动：**New + → Web Service** → 选 `campus-assistant` 仓库，然后按下面「手动配置」填。

**手动配置字段对照表**（两种方式都要检查这些）：
| 字段 | 填什么 |
|---|---|
| Name | `campus-assistant-backend` |
| Root Directory | `backend` |
| Runtime | **Docker**（自动用 `backend/Dockerfile`） |
| Plan | Free |
| Health Check Path | `/health` |

### 2.3 环境变量（关键）
在服务的 **Environment** 标签页，确认/添加：
| 变量名 | 值 | 说明 |
|---|---|---|
| `CORS_ORIGINS` | 先填 `*` 或 `https://campus-assistant.vercel.app` | 第 4 步会改成你真正的前端域名 |
| `PORT` | （留空/自动注入） | 不要手动设 |

其余（`VLM_PROVIDER`、`DASHSCOPE_API_KEY` 等）**都不用填**，默认 mock 模式即可跑通全链路。

### 2.4 等部署完成并记下地址
1. 点 **Deploy**，等 3～8 分钟（首次要装依赖）。
2. 部署成功后，服务页顶部会有一个公开地址，形如：
   ```
   https://campus-assistant-backend.onrender.com
   ```
3. 浏览器打开 `https://campus-assistant-backend.onrender.com/health`，返回 200/JSON 即成功。**把这个地址记下来**，下一步要用。

---

## 第 3 步：部署前端到 Vercel（约 10 分钟）

### 3.1 注册并登录
1. 打开 https://vercel.com ，点 **Sign Up**，选择 **Continue with GitHub** 授权登录。

### 3.2 导入项目
1. 点 **Add New → Project**。
2. 授权访问 GitHub 后，选择 `campus-assistant` 仓库，点 **Import**。

### 3.3 关键配置
1. **Root Directory**：点开编辑，改为 **`frontend`**。
2. 框架会自动识别为 **Vite**（`vercel.json` 已写好，不用改）。
3. 展开 **Environment Variables**，添加一条：
   | Name | Value |
   |---|---|
   | `VITE_API_BASE` | `https://campus-assistant-backend.onrender.com/api` |

   > ⚠️ 把上面 Value 里的域名换成你第 2 步实际拿到的 Render 地址，并且**结尾一定要带 `/api`**。

4. 点 **Deploy**，等 1～2 分钟。

### 3.4 记下前端地址
部署完成后，Vercel 会给你一个地址，形如：
```
https://campus-assistant.vercel.app
```
**把它记下来**，下一步要用。

---

## 第 4 步：回填跨域 + 验收（约 5 分钟）

### 4.1 回填 CORS
前端部署后，域名才确定。回到 **Render 控制台**：
1. 打开 `campus-assistant-backend` 服务 → **Environment**。
2. 把 `CORS_ORIGINS` 的值改成你**真正拿到的 Vercel 域名**（如 `https://campus-assistant.vercel.app`，不带斜杠结尾）。
3. 保存，Render 会自动重新部署（1～2 分钟）。

### 4.2 验收清单（逐项点一遍）
打开你的前端地址，测试：
- [ ] 页面能正常打开（不是白屏）。
- [ ] 粘贴一段课程通知文字（见下方示例），点处理。
- [ ] 能看到抽取出的「标题 / 发布方 / 截止时间」等字段。
- [ ] 「待办」看板出现对应任务。
- [ ] 「概览」页有统计数字。
- [ ] 「检索」里输入关键词能找到刚才那条通知。

**测试用文本**（直接复制）：
```
【课程通知】各位同学，计算系统导论课程作业3将于下周五 23:59 截止，
请按时在课程平台提交。作业要求详见课程主页，请提前安排时间完成。
—— 计算机学院 教务处
```

> 如果以上全部通过，你的在线演示就成功了。

---

## 第 5 步：补演示材料 + 交付（约 20 分钟，强烈建议）

招聘人员除了点链接，还会扫一眼你的 GitHub 首页。补三样东西，印象分拉满：

### 5.1 截图（3～4 张）
把下面几个界面各截一张，放进 README 顶部：
1. 概览仪表盘（统计卡片）
2. 上传/粘贴后识别出通知字段的界面
3. 待办看板（拖拽/状态流转）
4. 语义检索结果

### 5.2 录屏（30 秒，可选但加分）
按这个脚本录：粘贴通知文本 → 展示抽取结果 → 展示待办自动生成 → 检索一句「下周作业啥时候交」→ 出结果。

### 5.3 架构图
在 README 里放一张「通知识别 → 抽取 → 去重 → 待办 → 追踪」的流水线图（可让 AI 帮你画）。

### 5.4 更新 README
- 顶部加一行：「🔗 在线演示：https://campus-assistant.vercel.app」
- 加「架构图」和「界面截图」两个小节。

### 5.5 交付给招聘人员
给对方发这三样：
1. **在线演示链接**（点开即用）
2. **GitHub 仓库地址**（https://github.com/<你的用户名>/campus-assistant）
3. 一句话介绍（可参考）：
   > 这是一个「多模态校园事务智能助手」：上传课程通知/活动海报/作业要求（图片/PDF/文本），自动完成通知识别 → 关键信息抽取 → 待办生成 → 任务追踪，并用语义检索快速回查历史通知。后端 FastAPI + LangGraph 编排，前端 React + TypeScript，含 32 个自动化测试。

---

## 常见问题排查

| 现象 | 原因 & 解决 |
|---|---|
| push 报 `403` / `Authentication failed` | 令牌没填对。用户名填 GitHub 用户名，密码填令牌（不是账号密码）。 |
| push 报 `fatal: remote origin already exists` | 之前配过远程。执行 `git remote set-url origin https://github.com/<你的用户名>/campus-assistant.git` 覆盖。 |
| Render 部署失败 | 进服务页 → Logs 看红字。多半是依赖安装问题；确认 Root Directory 是 `backend`、Runtime 是 Docker。 |
| 前端白屏 / 接口 404 或 CORS 报错 | ① `VITE_API_BASE` 结尾没带 `/api`；② `CORS_ORIGINS` 没改成真实 Vercel 域名；③ 后端还在重新部署没起来。 |
| 首次打开很慢 | Render 免费版一段时间无访问会「休眠」，第一次打开要等几十秒，属正常。 |
| 想长期保存数据 | 免费版 SQLite 重启可能清空。演示无所谓；要持久化就在 Render 加 PostgreSQL 插件并把 `DATABASE_URL` 填进去。 |

## 替代平台（如遇免费层需验证/绑卡）
- 后端：**Railway**（连 GitHub 仓库，Root=`backend`，自动读 `backend/Procfile`）、**Fly.io**（用 `backend/Dockerfile`）。
- 前端：**Netlify**、**Cloudflare Pages**、**GitHub Pages**（只托管 `frontend/dist` 静态文件，均免费）。
- 配置文件和 Dockerfile 都是通用的，换平台同样能用。
