# --- core ---
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
pymupdf>=1.24          # PDF 文本层抽取 + 扫描页转图
numpy>=1.26
httpx>=0.27            # VLM / Embedding HTTP 调用
pillow>=10.0
pytest>=8.0

# --- optional: 有则自动启用，无则走内置兜底 ---
langgraph>=0.2         # 无此包时 pipeline 退化为顺序执行（逻辑等价）
faiss-cpu>=1.8         # 无此包时向量检索退化为 numpy 余弦相似度
psycopg[binary]>=3.1   # 生产切 PostgreSQL 时需要
paddleocr>=2.7         # 可选 OCR 引擎（Windows 依赖较重）
paddlepaddle>=2.6
rapidocr-onnxruntime>=1.3  # 轻量 OCR 兜底引擎
